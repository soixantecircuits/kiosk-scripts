Once Screenshotmonitor is launched in your system launch the below command to have it run then hide once it is started.
Requirements:
sudo apt-get install xdotool
sudo apt-get install npm or nvm
npm install pm2 -g

Command:
bash kiosk-sm.sh
